name: sns_repository
description: A Very Good Project created by Very Good CLI.
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  equatable: ^2.0.5
  json_annotation: ^4.9.0
  sns_client:
    path: ../sns_client
  sns_mqtt_client:
    path: ../sns_mqtt_client

dev_dependencies:
  build_runner: ^2.4.11
  json_serializable: ^6.8.0
  mocktail: ^1.0.4
  test: ^1.25.7
  very_good_analysis: ^6.0.0
